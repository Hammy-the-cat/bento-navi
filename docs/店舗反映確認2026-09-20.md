# 店舗シート反映確認（2026-09-20）

## 照合・公開結果

| 項目 | 結果 |
| --- | --- |
| Google Sheets | 12冊、店舗タブ38、11,025行 |
| ID重複・未分類行 | 0件 |
| アプリ掲載 | 11,022件 |
| うち地図・距離検索 | 11,015件 |
| うち地域別一覧のみ | 7件（座標なし） |
| 閉店・閉店予定で除外 | 3件 |
| 既存の補完済み座標を保持 | 169件（同住所のみ） |
| 従来11,015件の情報変更 | 都道府県項目の追加のみ。既存情報はすべて維持 |
| Web公開 | 完了、gh-pages `96b8328` |
| 公開データSHA-256 | `34dff90866e7b2a243a46ca27b4e20f9b62ee28394324964fa18ccfe967387b5` |
| 公開データと監査結果 | 完全一致をHTTP取得で確認 |
| 検証 | Flutter 33テスト、Node 6テスト、静的解析、Web本番ビルド成功 |

公開先：[べんとうナビ](https://bento.hammythecat.com/)。ホーム画面「地域別の掲載店から探す」から全掲載店を検索できる。住所非公開の「甘雨」の検索・詳細表示、地図ボタン非表示をブラウザで確認。

シートの既存値は変更していない。関東7都県・熊本県・沖縄県のシートは今回の接続先では見つからなかった。これらの地域の店舗調査が完了したことを意味しない。

## iOSの残作業

ソースは `feature/sheet-sync-audit` / `b452afd` に保存し、次版 `1.3.1+17` を準備済み。現在のiOS版にはこの変更はまだ入っていない。

CodemagicのChromeタブへ接続を2回試したが、画面操作接続がタイムアウトしたためビルド開始に至っていない。再接続後に、以下を実施する。

1. Codemagic → bento-navi → Start new build。
2. Branch `feature/sheet-sync-audit`、Workflow `iOS リリース (TestFlight)` を選択。
3. ビルドとApp Store Connectへのアップロード成功を確認。
4. TestFlightで「地域別の掲載店から探す」、宮崎県／「甘雨」検索、従来の周辺検索を実機確認。
5. 現在審査待ちの1.3.0の状態を確認し、次版1.3.1として更新する。1.3.0を取り下げる操作は行っていない。

今後は [店舗シート同期手順](店舗シート同期手順.md) で全行照合して更新する。自動監視・自動配信は未導入。

今回の照合用データは `../tmp/bento-sheet-audit-20260920/` に保存。新しいresearchフォルダは作成しておらず、未完了調査のキャッシュも削除していない。今回の整理による削減容量は0バイト。
