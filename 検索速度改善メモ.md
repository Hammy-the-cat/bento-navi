# 検索速度の改善（2026年9月15日）

## 変更内容

- 調査済み店舗がある場合は、外部サービスの完了を待たず先に表示します。外部の結果は到着後に追加します。
- 同じ座標・半径の検索結果を5分間、最大30件までメモリに保存します。通信に失敗した結果は保存しません。
- 成功した会場名検索を最大30件、画面の存続中に再利用します。アプリを終了すると消えます。
- 約8MBの店舗JSONをスマートフォンでは別のisolateで解析し、同時読み込みをまとめます。
- 調査済み店舗がない場合の外部検索は、1接続先あたり20秒から6秒へ待機上限を変更しました。3接続先がすべて無応答の場合の待機上限は約60秒から約18秒になります。応答が6秒を超える接続先では次の接続先へ進むため、混雑時に検索が失敗する可能性は残ります。
- 会場名検索の各通信に10秒の待機上限を追加しました。
- 検索やリセットの後に古い店舗結果が届いても画面を上書きしません。追加読み込み時は選択済みのカテゴリを維持します。

## 検証と実機確認

全27テストが通過し、静的解析も指摘なし。追加テストでは外部通信完了前の結果通知、再検索の通信省略、半径変更時の再取得、外部検索失敗時の店舗維持、会場名の再利用を確認しました。

実機の所要時間は未測定です。TestFlightで、調査済み店舗のある地域・ない地域、会場名・現在地・履歴からの検索を確認してください。初回表示と外部店舗追加の時間を分けて測定します。再検索、電波が弱い場合、読み込み中のリセットも確認対象です。

この変更はローカルのソースコードに適用したものです。App Store公開版への反映には、macOSまたは既存のiOSビルド環境で更新ビルドを作成し、実機確認後に配信する必要があります。
